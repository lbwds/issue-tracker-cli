# Issue Tracker CLI - 使用指导

> **版本**: 2.0
> **日期**: 2026-02-04
> **适用于**: 支持多项目的通用 Issue 追踪工具

---

## 目录

1. [快速开始](#1-快速开始)
2. [命令详解](#2-命令详解)
3. [日常工作流](#3-日常工作流)
4. [高级用法](#4-高级用法)
5. [故障排查](#5-故障排查)
6. [新项目接入指南](#6-新项目接入指南)

---

## 1. 快速开始

### 1.1 安装

```bash
# 从 PyPI 安装
pip install issue-tracker-cli

# 或从 GitHub 安装
pip install git+ssh://git@github.com/lbwds/issue-tracker-cli.git@v1.2.2
```

### 1.2 推荐方式：项目目录配置

在你的项目根目录创建 `config.yaml`：

```bash
cd /path/to/your/project

cat > config.yaml << 'EOF'
project:
  id: "001"
  name: "YourProject"

id_rules:
  format: "{num:03d}"

priorities: [P0, P1, P2, P3]
statuses: [pending, in_progress, planned, fixed, n_a]

github:
  enabled: true
  close_on_fix: true
  comment_template: "自动同步: {issue_id} 已修复"

export:
  output: "exports/issues.md"
EOF
```

然后直接在项目目录中使用命令：

```bash
# 新增问题（编号自动分配）
issue-tracker add --title "登录功能异常" --priority P0 --description "用户无法正常登录"

# 查看统计
issue-tracker stats

# 查询待处理问题
issue-tracker query --status pending
```

**优势**：无需每次指定 `-p` 参数，在项目目录中直接使用即可。

---

### 1.3 其他使用方式

#### 方式 A: 使用 `-p` 参数管理多个项目

```bash
# 设置环境变量（可选）
export ISSUE_TRACKER_HOME=$HOME/issue-tracker-cli
mkdir -p $ISSUE_TRACKER_HOME/.config

# 创建项目配置（文件名格式: {project_id}_{name}.yaml）
cat > $ISSUE_TRACKER_HOME/.config/001_ProjectA.yaml << 'EOF'
project:
  id: "001"
  name: "ProjectA"
...
EOF

# 使用 -p 参数切换项目
issue-tracker -p 001 add --title "..." --priority P2
issue-tracker -p 002 add --title "..." --priority P1
```

#### 方式 B: 使用 `-c` 参数指定配置路径

```bash
issue-tracker -c /path/to/config.yaml add --title "..." --priority P2
```

---

### 1.4 配置文件查找逻辑

工具按以下优先级查找配置文件：

```
1. -c <config> 参数         → 直接使用指定路径
2. -p <project_id> 参数     → 搜索 ISSUE_TRACKER_HOME/.config/{project_id}_*.yaml
3. (无参数) 自动查找:
   a) 当前目录/config.yaml   → 推荐方式，适合单项目使用
   b) .config/ 唯一配置文件  → 多项目时自动使用唯一配置
   c) git root/config.yaml   → 兼容旧模式
```

---

## 2. 命令详解

### 2.1 项目切换

```bash
# 通过 project_id 切换
issue-tracker -p 001 stats
issue-tracker -p 002 query

# 手动指定配置文件
issue-tracker -c /path/to/config.yaml query
```

### 2.2 add - 新增问题

**语法**:
```bash
issue-tracker [-p PROJECT_ID] add \
    [--id <编号>] \
    --title "<标题>" \
    --priority <优先级> \
    [--status <状态>] \
    [--discovery-date <日期>] \
    [--file "<路径>"] \
    [--location "<位置>"] \
    [--description "<描述>"] \
    [--impact "<影响>"] \
    [--fix-plan "<方案>"] \
    [--estimated-hours <小时>] \
    [--phase <阶段>] \
    [--github-issue-id <编号>]
```

**参数说明**:

| 参数 | 必填 | 说明 | 示例 |
|------|------|------|------|
| `-p PROJECT_ID` | ❌ | 项目编号 | `001` |
| `--id` | ❌ | 问题编号（纯数字，默认自动分配） | `001` |
| `--title` | ✅ | 问题标题 | `"内存泄漏风险"` |
| `--priority` | ✅ | 优先级 (P0/P1/P2/P3) | `P2` |
| `--status` | ❌ | 状态 | 默认 `pending` |
| `--discovery-date` | ❌ | 发现日期 | 默认今天 |
| `--file` | ❌ | 文件路径 | `"src/core/test.cpp"` |
| `--location` | ❌ | 位置描述 | `"行 10-15"` |
| `--description` | ❌ | 问题描述 | |
| `--impact` | ❌ | 影响 | |
| `--fix-plan` | ❌ | 修复方案 | |
| `--estimated-hours` | ❌ | 预计工时 | `2.5` |
| `--actual-hours` | ❌ | 实际工时 | `2.0` |
| `--phase` | ❌ | 所属阶段 | `phase3` |
| `--github-issue-id` | ❌ | GitHub Issue 编号 | `123` |

**示例**:
```bash
# 新增 Medium 优先级问题（编号自动分配）
issue-tracker -p 001 add \
    --title "函数参数校验缺失" \
    --priority P2 \
    --file "src/core/Config.cpp" \
    --location "行 100" \
    --description "缺少对空指针的检查" \
    --estimated-hours 1

# 手动指定编号
issue-tracker -p 001 add \
    --id 999 \
    --title "特殊编号问题" \
    --priority P3
```

### 2.3 update - 更新问题

**语法**:
```bash
issue-tracker [-p PROJECT_ID] update <编号> \
    [--title "<新标题>"] \
    [--priority <优先级>] \
    [--status <状态>] \
    [--fix-date <日期>] \
    [--actual-hours <小时>] \
    [--github-issue-id <编号>] \
    ...
```

**常用更新场景**:

```bash
# 标记为已修复
issue-tracker -p 001 update 001 \
    --status fixed \
    --fix-date 2026-02-04 \
    --actual-hours 1.5

# 关联 GitHub Issue
issue-tracker -p 001 update 001 --github-issue-id 456

# 更新标题和描述
issue-tracker -p 001 update 001 \
    --title "更准确的标题" \
    --description "更详细的描述"
```

### 2.4 query - 多条件查询

**语法**:
```bash
issue-tracker [-p PROJECT_ID] query \
    [--id <编号>] \
    [--priority <优先级>] \
    [--status <状态>] \
    [--phase <阶段>] \
    [--file <glob模式>] \
    [--github <Issue编号>] \
    [--detail]
```

**示例**:

```bash
# 查询所有 P2 待修复问题
issue-tracker -p 001 query --priority P2 --status pending

# 查询某文件相关问题
issue-tracker -p 001 query --file "src/hal/*"

# 查询某 GitHub Issue 关联的本地问题
issue-tracker -p 001 query --github 456

# 详细模式（展开描述）
issue-tracker -p 001 query --id 001 --detail

# 组合查询
issue-tracker -p 001 query --priority P0 --status pending --detail
```

### 2.5 list - 简洁列表

**语法**:
```bash
issue-tracker [-p PROJECT_ID] list \
    [--status <状态>] \
    [--priority <优先级>]
```

**输出格式**:
```
  编号     问题描述                                                优先级     发现日期          状态
  -----  --------------------------------------------------  ------  ------------  --------
  001    SystemStateMachine::saveState 使用 system() 执行 mkd..  P2      今天          ❌ 待修复
  002    RecipeData::fromJson 枚举值 static_cast 无范围校验          P2      2026-02-01    ❌ 待修复
```

### 2.6 stats - 统计概览

**语法**:
```bash
issue-tracker [-p PROJECT_ID] stats
```

**输出示例**:
```
==================================================
  MyProject - 问题统计
==================================================
  总数: 132

  按优先级:
  优先级           总数    已修复    待处理     进度
  ---------- ----- ------ ------ ------
  P0            23     23      0   100%
  P1            26     26      0   100%
  P2            45     39      5    86%
  P3            38     27     11    71%

  按状态:
    fixed            115  ██████████████████████████
    pending           11  ██
    planned            4
    in_progress        1
    n_a                1
==================================================
```

### 2.7 export - 生成 Markdown

**语法**:
```bash
issue-tracker [-p PROJECT_ID] export [--output <路径>]
```

**默认输出**: `ISSUE_TRACKER_HOME/exports/xxx.md`（配置文件中指定）

**示例**:
```bash
# 导出到默认位置
issue-tracker -p 001 export

# 导出到指定位置
issue-tracker -p 001 export --output /tmp/issues.md
```

### 2.8 sync - GitHub 同步

**语法**:
```bash
issue-tracker [-p PROJECT_ID] sync [--dry-run]
```

**同步逻辑**:
- 查找 `status=fixed` 且有关联 `github_issue_id` 的条目
- 调用 `gh issue close` 自动关闭
- 记录同步日志，避免重复操作

**示例**:
```bash
# 预览（不实际执行）
issue-tracker -p 001 sync --dry-run

# 实际同步
issue-tracker -p 001 sync
```

**输出示例**:
```
待同步条目: 2 条
------------------------------------------------------------
  001 → GitHub Issue #456 (标题: 示例问题)
    ✓ 已关闭 GitHub Issue #456
  002 → GitHub Issue #457 (标题: 另一个问题)
    ✓ 已关闭 GitHub Issue #457
------------------------------------------------------------
同步完成: 2 成功, 0 失败
```

### 2.9 migrate - 导入数据

**语法**:
```bash
issue-tracker [-p PROJECT_ID] migrate \
    --source <源文件> \
    --migrator <migrator名称> \
    [--force] \
    [--dry-run]
```

**参数说明**:

| 参数 | 说明 |
|------|------|
| `--source` | 源文件路径 |
| `--migrator` | migrator 名称 (如 `weldsmart`) |
| `--force` | 强制覆盖已有数据 |
| `--dry-run` | 仅解析，不写入数据库 |

**注意**: 迁移时会自动重新分配编号为全局递增序号。

**示例**:
```bash
# 首次导入
issue-tracker -p 001 migrate \
    --source all-issues.md \
    --migrator weldsmart

# 强制刷新（覆盖已有数据）
issue-tracker -p 001 migrate \
    --source all-issues.md \
    --migrator weldsmart \
    --force

# 预览解析结果
issue-tracker -p 001 migrate \
    --source all-issues.md \
    --migrator weldsmart \
    --dry-run
```

---

## 3. 日常工作流

### 3.1 典型工作流程

```
┌─────────────────────────────────────────────────────────────────┐
│                        开发周期                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 拉取最新代码                                                 │
│     ↓                                                           │
│  2. (可选) migrate 同步 MD → DB                                 │
│     ↓                                                           │
│  3. 开发过程中使用 query/list/stats 查询                         │
│     ↓                                                           │
│  4. 新增问题: add --title "..." --priority Px                   │
│     ↓                                                           │
│  5. 修复问题后 update 更新状态                                   │
│     ↓                                                           │
│  6. commit 前执行 export (DB → MD)                              │
│     ↓                                                           │
│  7. git add/commit/push                                         │
│     ↓                                                           │
│  8. (可选) sync 关闭 GitHub Issue                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 场景操作清单

| 场景 | 操作 |
|------|------|
| **首次使用** | 创建配置 → `add` 新增问题 → `stats` 验证 |
| **拉取最新后** | `migrate` (同步 MD → DB，不覆盖已有修改) |
| **查找问题** | `query --status pending --priority P2` |
| **新增问题** | `add --title "..." --priority Px` |
| **修复完成** | `update <id> --status fixed --fix-date today` |
| **提交前** | `export` → 生成新的 issues.md |
| **同步 GitHub** | `sync --dry-run` → `sync` |
| **统计报告** | `stats` |

### 3.3 Git 工作流示例

```bash
# 1. 拉取最新
git pull

# 2. (可选) 同步问题清单（如果有更新）
issue-tracker -p 001 migrate \
    --source exports/issues.md --migrator weldsmart

# 3. 开发过程中查询问题
issue-tracker -p 001 query --status pending

# 4. 新增问题
issue-tracker -p 001 add \
    --title "新发现的问题" \
    --priority P2 \
    --file "src/core/test.cpp"

# 5. 修复后更新状态
issue-tracker -p 001 update 001 \
    --status fixed \
    --fix-date 2026-02-04 \
    --actual-hours 2

# 6. 提交前导出
issue-tracker -p 001 export

# 7. 提交
git add exports/issues.md
git commit -m "fix: 修复 001 函数参数校验缺失

Fixes #456"

# 8. 同步 GitHub（关闭已修复的 Issue）
issue-tracker -p 001 sync

# 9. 推送
git push
```

---

## 4. 高级用法

### 4.1 直接使用 SQLite

数据库可直接用 `sqlite3` 命令访问：

```bash
# 进入数据库
sqlite3 ~/issue-tracker-cli/data/001_MyProject.db

# 查询待处理问题
SELECT * FROM v_pending;

# 统计按模块分组
SELECT
    CASE
        WHEN file_path LIKE 'src/core%' THEN 'Core'
        WHEN file_path LIKE 'src/hal%' THEN 'HAL'
        WHEN file_path LIKE 'src/business%' THEN 'Business'
        ELSE 'Other'
    END AS module,
    COUNT(*) AS count
FROM issues
GROUP BY module;

# 退出
.exit
```

### 4.2 编写自定义查询脚本

```python
#!/usr/bin/env python3
import sqlite3

conn = sqlite3.connect('~/issue-tracker-cli/data/001_MyProject.db')
conn.row_factory = sqlite3.Row

# 查询耗时超过预计的问题
cur = conn.execute('''
    SELECT id, title, estimated_hours, actual_hours
    FROM issues
    WHERE actual_hours > estimated_hours
    AND actual_hours IS NOT NULL
''')

print("超时问题:")
for row in cur:
    print(f"  {row['id']}: 预计{row['estimated_hours']}h 实际{row['actual_hours']}h")

conn.close()
```

### 4.3 批量更新

```bash
# 批量标记为已修复（使用 sqlite3）
sqlite3 ~/issue-tracker-cli/data/001_*.db \
    "UPDATE issues SET status='fixed', fix_date='2026-02-04' WHERE id IN ('001', '002', '003');"
```

### 4.4 配置环境变量持久化

在 `~/.bashrc` 或 `~/.zshrc` 中添加：

```bash
# Issue Tracker 配置
export ISSUE_TRACKER_HOME=$HOME/issue-tracker-cli
```

### 4.5 配置 Shell 别名

在 `~/.bashrc` 或 `~/.zshrc` 中添加：

```bash
# Issue Tracker 别名
alias iss='issue-tracker -p 001'
alias issq='iss query'
alias issl='iss list'
alias issp='iss query --status pending'
alias issf='iss query --status fixed'
alias iss-stats='iss stats'
```

使用：
```bash
issq --priority P2 --status pending
iss-stats
```

---

## 5. 故障排查

### 5.1 常见错误

#### 错误: `项目查找失败: 未找到项目 'xxx' 的配置文件`

**原因**: 配置文件不存在

**解决**:
```bash
# 检查配置目录
ls $ISSUE_TRACKER_HOME/.config/

# 创建配置文件
cat > $ISSUE_TRACKER_HOME/.config/001_MyProject.yaml << 'EOF'
project:
  id: "001"
  name: "MyProject"
...
EOF
```

#### 错误: `编号 'abc' 格式无效`

**原因**: 编号必须为纯数字

**解决**:
```bash
# 正确：使用纯数字编号
issue-tracker -p 001 add --id 123 --title "..." --priority P2

# 或者不指定编号，让系统自动分配
issue-tracker -p 001 add --title "..." --priority P2
```

#### 错误: `优先级 'Px' 无效`

**原因**: 配置文件中未定义该优先级

**解决**: 编辑配置文件，添加对应优先级：
```yaml
priorities: [P0, P1, P2, P3, Px]
```

#### 错误: `编号 '001' 已存在`

**原因**: 尝试添加重复编号

**解决**:
- 使用 `update` 更新现有条目
- 或不指定编号，让系统自动分配

#### 错误: `gh 命令失败`

**原因**: `gh` CLI 未安装或未登录

**解决**:
```bash
# 安装 gh
# https://cli.github.com/

# 登录
gh auth login
```

### 5.2 数据库损坏

如果数据库损坏，重新迁移：

```bash
# 删除旧数据库
rm ~/issue-tracker-cli/data/001_*.db

# 重新迁移
issue-tracker -p 001 migrate \
    --source exports/issues.md --migrator weldsmart
```

### 5.3 迁移结果不一致

如果迁移条目数与预期不符：

```bash
# 1. 检查警告
issue-tracker -p 001 migrate \
    --source exports/issues.md --migrator weldsmart --dry-run

# 2. 检查数据库
sqlite3 ~/issue-tracker-cli/data/001_*.db "SELECT COUNT(*) FROM issues;"

# 3. 对比源文件
grep -c "^### [0-9]" exports/issues.md
```

---

## 6. 新项目接入指南

### 6.1 创建项目配置

```bash
# 在 ISSUE_TRACKER_HOME/.config/ 创建配置
# 文件命名格式: {project_id}_{project_name}.yaml

cat > $ISSUE_TRACKER_HOME/.config/002_NewProject.yaml << 'EOF'
project:
  id: "002"
  name: "NewProject"

id_rules:
  format: "{num:03d}"

priorities: [P0, P1, P2, P3]
statuses: [pending, in_progress, fixed, n_a]

github:
  enabled: true
  close_on_fix: true
  comment_template: "Auto-sync: {issue_id} fixed"

export:
  output: "exports/newproject_issues.md"
EOF
```

### 6.2 验证配置

```bash
# 尝试访问项目
issue-tracker -p 002 stats

# 应显示空统计
==================================================
  NewProject - 问题统计
==================================================
  总数: 0
...
```

### 6.3 新增问题测试

```bash
issue-tracker -p 002 add \
    --title "测试问题" \
    --priority P2 \
    --description "这是一个测试问题"
```

### 6.4 实现 Migrator 插件（可选）

如果需要从现有数据迁移，创建自定义 migrator：

```python
# migrators/custom_migrator.py
from . import BaseMigrator
import re

class CustomMigrator(BaseMigrator):
    def parse(self, source_path: str) -> list[dict]:
        with open(source_path, 'r', encoding='utf-8') as f:
            content = f.read()

        issues = []
        # 实现项目特定的解析逻辑
        # ...

        return issues

    def validate(self, issues: list[dict]) -> list[str]:
        warnings = []
        # 校验逻辑
        return warnings
```

注册到 `cli.py` 的 `_load_migrator` 函数：

```python
def _load_migrator(name: str):
    if name == "weldsmart":
        return WeldSmartMigrator()
    if name == "custom":
        from issue_tracker.migrators.custom_migrator import CustomMigrator
        return CustomMigrator()
    return None
```

### 6.5 测试迁移

```bash
# Dry-run 预览
issue-tracker -p 002 migrate \
    --source your-issues.md \
    --migrator custom \
    --dry-run

# 实际导入
issue-tracker -p 002 migrate \
    --source your-issues.md \
    --migrator custom
```

---

## 7. 附录

### 7.1 状态符号对照表

| 符号 | 状态值 | 含义 |
|------|--------|------|
| ✅ | `fixed` | 已修复/已完成 |
| ❌ | `pending` | 待修复 |
| 🟢 | `in_progress` | 进行中 |
| 📋 | `planned` | 待规划 |
| ⚠️ | `n_a` | 不适用 |

### 7.2 优先级说明

| 优先级 | 含义 |
|--------|------|
| P0 | 紧急 - 影响核心功能正常运行 |
| P1 | 高 - 严重影响代码质量和安全性 |
| P2 | 中 - 影响代码可维护性和健壮性 |
| P3 | 低 - 代码风格和最佳实践 |

### 7.3 文件路径

| 文件/目录 | 路径 | 说明 |
|-----------|------|------|
| 配置目录 | `ISSUE_TRACKER_HOME/.config/` | 项目配置文件存储 |
| 数据库 | `ISSUE_TRACKER_HOME/data/` | 本地数据库存储 |
| 导出文件 | `ISSUE_TRACKER_HOME/exports/` | Markdown 导出文件 |

### 7.4 命令速查表

| 命令 | 功能 | 常用参数 |
|------|------|---------|
| `add` | 新增 | `--title`, `--priority` |
| `update` | 更新 | `--status`, `--fix-date` |
| `query` | 查询 | `--status`, `--priority`, `--detail` |
| `list` | 列表 | `--status` |
| `stats` | 统计 | 无 |
| `export` | 导出 | `--output` |
| `sync` | 同步 | `--dry-run` |
| `migrate` | 导入 | `--source`, `--migrator`, `--force` |

---

**文档结束**

如有问题，请参考 `技术设计方案.md` 或提交 Issue。

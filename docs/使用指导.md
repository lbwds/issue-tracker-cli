# Issue Tracker CLI - 使用指导

> **版本**: 1.0
> **日期**: 2026-02-03
> **适用于**: WeldSmart Pro V3.2 及其他采用 `config.yaml + migrator` 模式的项目

---

## 目录

1. [快速开始](#1-快速开始)
2. [命令详解](#2-命令详解)
3. [日常工作流](#3-日常工作流)
4. [高级用法](#4-高级用法)
5. [故障排查](#5-故障排查)
6. [新项目接入指南](#6-新项目接入指南)

---

## 1. 快速开始

### 1.1 安装依赖

```bash
# 确保 Python 3.10+
python3 --version

# 安装 PyYAML
pip install pyyaml

# (可选) GitHub CLI - 用于 GitHub 同步功能
# https://cli.github.com/
gh --version
```

### 1.2 首次使用 - 迁移导入

```bash
cd /path/to/WeldSmart_Pro

# 从 all-issues.md 导入到数据库
python3 scripts/issue-tracker/issue_manager.py \
    migrate --source docs/project/all-issues.md --migrator weldsmart
```

**输出示例**:
```
解析: docs/project/all-issues.md
解析得到: 132 条
迁移完成: 插入 132 条, 跳过 0 条
```

### 1.3 验证导入

```bash
# 查看统计
python3 scripts/issue-tracker/issue_manager.py stats

# 查询待处理问题
python3 scripts/issue-tracker/issue_manager.py query --status pending
```

---

## 2. 命令详解

### 2.1 add - 新增问题

**语法**:
```bash
python3 issue_manager.py add \
    --id <编号> \
    --title "<标题>" \
    [--priority <优先级>] \
    [--status <状态>] \
    [--discovery-date <日期>] \
    [--file "<路径>"] \
    [--location "<位置>"] \
    [--description "<描述>"] \
    [--impact "<影响>"] \
    [--fix-plan "<方案>"] \
    [--estimated-hours <小时>] \
    [--phase <阶段>] \
    [--github-issue-id <编号>]
```

**参数说明**:

| 参数 | 必填 | 说明 | 示例 |
|------|------|------|------|
| `--id` | ✅ | 问题编号 | `M-037` |
| `--title` | ✅ | 问题标题 | `"内存泄漏风险"` |
| `--priority` | ❌ | 优先级 (P0/P1/P2/P3) | 默认根据前缀推断 |
| `--status` | ❌ | 状态 | 默认 `pending` |
| `--discovery-date` | ❌ | 发现日期 | 默认今天 |
| `--file` | ❌ | 文件路径 | `"src/core/test.cpp"` |
| `--location` | ❌ | 位置描述 | `"行 10-15"` |
| `--description` | ❌ | 问题描述 | |
| `--impact` | ❌ | 影响 | |
| `--fix-plan` | ❌ | 修复方案 | |
| `--estimated-hours` | ❌ | 预计工时 | `2.5` |
| `--actual-hours` | ❌ | 实际工时 | `2.0` |
| `--phase` | ❌ | 所属阶段 | `phase3` |
| `--github-issue-id` | ❌ | GitHub Issue 编号 | `123` |

**示例**:
```bash
# 新增 Medium 优先级问题
python3 issue_manager.py add \
    --id M-037 \
    --title "函数参数校验缺失" \
    --priority P2 \
    --file "src/core/Config.cpp" \
    --location "行 100" \
    --description "缺少对空指针的检查" \
    --estimated-hours 1
```

### 2.2 update - 更新问题

**语法**:
```bash
python3 issue_manager.py update <编号> \
    [--title "<新标题>"] \
    [--status <状态>] \
    [--fix-date <日期>] \
    [--actual-hours <小时>] \
    [--github-issue-id <编号>] \
    ...
```

**常用更新场景**:

```bash
# 标记为已修复
python3 issue_manager.py update M-037 \
    --status fixed \
    --fix-date 2026-02-03 \
    --actual-hours 1.5

# 关联 GitHub Issue
python3 issue_manager.py update M-037 --github-issue-id 456
```

### 2.3 query - 多条件查询

**语法**:
```bash
python3 issue_manager.py query \
    [--id <编号>] \
    [--priority <优先级>] \
    [--status <状态>] \
    [--phase <阶段>] \
    [--file <glob模式>] \
    [--github <Issue编号>] \
    [--detail]
```

**示例**:

```bash
# 查询所有 P2 待修复问题
python3 issue_manager.py query --priority P2 --status pending

# 查询某文件相关问题
python3 issue_manager.py query --file "src/hal/*"

# 查询某 GitHub Issue 关联的本地问题
python3 issue_manager.py query --github 456

# 详细模式（展开描述）
python3 issue_manager.py query --id M-037 --detail
```

### 2.4 list - 简洁列表

**语法**:
```bash
python3 issue_manager.py list \
    [--status <状态>] \
    [--priority <优先级>]
```

**输出格式**:
```
  编号     问题描述                                                优先级     发现日期          状态
  -----  --------------------------------------------------  ------  ------------  --------
  M-037  SystemStateMachine::saveState 使用 system() 执行 mkd..  P2      2026-02-03    ❌ 待修复
  M-038  RecipeData::fromJson 枚举值 static_cast 无范围校验          P2      2026-02-03    ❌ 待修复
```

### 2.5 stats - 统计概览

**语法**:
```bash
python3 issue_manager.py stats
```

**输出示例**:
```
==================================================
  WeldSmart Pro - 问题统计
==================================================
  总数: 132

  按优先级:
  优先级           总数    已修复    待处理     进度
  ---------- ----- ------ ------ ------
  P0            23     23      0   100%
  P1            26     26      0   100%
  P2            45     39      5    86%
  P3            38     27     11    71%
  A(Arch)        4      4      0   100%
  T(Test)        2      0      2     0%

  按状态:
    fixed            115  ██████████████████████████
    pending           11  ██
    planned            4
    in_progress        1
    n_a                1
==================================================
```

### 2.6 export - 生成 Markdown

**语法**:
```bash
python3 issue_manager.py export [--output <路径>]
```

**默认输出**: `config.yaml` 中 `export.output` 指定的路径

**示例**:
```bash
# 导出到默认位置
python3 issue_manager.py export

# 导出到指定位置
python3 issue_manager.py export --output /tmp/issues.md
```

### 2.7 sync - GitHub 同步

**语法**:
```bash
python3 issue_manager.py sync [--dry-run]
```

**同步逻辑**:
- 查找 `status=fixed` 且有关联 `github_issue_id` 的条目
- 调用 `gh issue close` 自动关闭
- 记录同步日志，避免重复操作

**示例**:
```bash
# 预览（不实际执行）
python3 issue_manager.py sync --dry-run

# 实际同步
python3 issue_manager.py sync
```

**输出示例**:
```
待同步条目: 2 条
------------------------------------------------------------
  M-037 → GitHub Issue #456 (标题: 示例问题)
    ✓ 已关闭 GitHub Issue #456
  M-038 → GitHub Issue #457 (标题: 另一个问题)
    ✓ 已关闭 GitHub Issue #457
------------------------------------------------------------
同步完成: 2 成功, 0 失败
```

### 2.8 migrate - 导入数据

**语法**:
```bash
python3 issue_manager.py migrate \
    --source <源文件> \
    --migrator <migrator名称> \
    [--force] \
    [--dry-run]
```

**参数说明**:

| 参数 | 说明 |
|------|------|
| `--source` | 源文件路径 |
| `--migrator` | migrator 名称 (如 `weldsmart`) |
| `--force` | 强制覆盖已有数据 |
| `--dry-run` | 仅解析，不写入数据库 |

**示例**:
```bash
# 首次导入
python3 issue_manager.py migrate \
    --source docs/project/all-issues.md \
    --migrator weldsmart

# 强制刷新（覆盖已有数据）
python3 issue_manager.py migrate \
    --source docs/project/all-issues.md \
    --migrator weldsmart \
    --force

# 预览解析结果
python3 issue_manager.py migrate \
    --source docs/project/all-issues.md \
    --migrator weldsmart \
    --dry-run
```

---

## 3. 日常工作流

### 3.1 典型工作流程

```
┌─────────────────────────────────────────────────────────────────┐
│                        开发周期                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 拉取最新代码                                                 │
│     ↓                                                           │
│  2. migrate (同步 MD → DB)                                      │
│     ↓                                                           │
│  3. 开发过程中使用 query/list/stats 查询                         │
│     ↓                                                           │
│  4. 修复问题后 update 更新状态                                   │
│     ↓                                                           │
│  5. commit 前执行 export (DB → MD)                              │
│     ↓                                                           │
│  6. git add/commit/push                                         │
│     ↓                                                           │
│  7. (可选) sync 关闭 GitHub Issue                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 场景操作清单

| 场景 | 操作 |
|------|------|
| **首次使用** | `migrate` → `stats` 验证 |
| **拉取最新后** | `migrate` (不覆盖已有修改) |
| **查找问题** | `query --status pending --priority P2` |
| **新增问题** | `add --id M-XXX --title "..."` |
| **修复完成** | `update M-XXX --status fixed --fix-date today` |
| **提交前** | `export` → 生成新的 `all-issues.md` |
| **同步 GitHub** | `sync --dry-run` → `sync` |
| **统计报告** | `stats` |

### 3.3 Git 工作流示例

```bash
# 1. 拉取最新
git pull

# 2. 同步问题清单（如果有更新）
python3 scripts/issue-tracker/issue_manager.py migrate \
    --source docs/project/all-issues.md --migrator weldsmart

# 3. 开发过程中查询问题
python3 scripts/issue-tracker/issue_manager.py query --status pending

# 4. 修复后更新状态
python3 scripts/issue-tracker/issue_manager.py update M-037 \
    --status fixed --fix-date 2026-02-03 --actual-hours 2

# 5. 提交前导出
python3 scripts/issue-tracker/issue_manager.py export

# 6. 提交
git add docs/project/all-issues.md
git commit -m "fix(business): 修复 M-037 函数参数校验缺失

Fixes #456"

# 7. 同步 GitHub（关闭已修复的 Issue）
python3 scripts/issue-tracker/issue_manager.py sync

# 8. 推送
git push
```

---

## 4. 高级用法

### 4.1 直接使用 SQLite

数据库可直接用 `sqlite3` 命令访问：

```bash
# 进入数据库
sqlite3 docs/project/issues.db

# 查询待处理问题
SELECT * FROM v_pending;

# 统计按模块分组
SELECT
    CASE
        WHEN file_path LIKE 'src/core%' THEN 'Core'
        WHEN file_path LIKE 'src/hal%' THEN 'HAL'
        WHEN file_path LIKE 'src/business%' THEN 'Business'
        ELSE 'Other'
    END AS module,
    COUNT(*) AS count
FROM issues
GROUP BY module;

# 退出
.exit
```

### 4.2 编写自定义查询脚本

```python
#!/usr/bin/env python3
import sqlite3

conn = sqlite3.connect('docs/project/issues.db')
conn.row_factory = sqlite3.Row

# 查询耗时超过预计的问题
cur = conn.execute('''
    SELECT id, title, estimated_hours, actual_hours
    FROM issues
    WHERE actual_hours > estimated_hours
    AND actual_hours IS NOT NULL
''')

print("超时问题:")
for row in cur:
    print(f"  {row['id']}: 预计{row['estimated_hours']}h 实际{row['actual_hours']}h")

conn.close()
```

### 4.3 批量更新

```bash
# 批量标记为已修复（使用 sqlite3）
sqlite3 docs/project/issues.db \
    "UPDATE issues SET status='fixed', fix_date='2026-02-03' WHERE id IN ('M-037', 'M-038', 'M-039');"
```

### 4.4 配置自定义别名

在 `~/.bashrc` 或 `~/.zshrc` 中添加：

```bash
# Issue Tracker 别名
alias iss='python3 ~/Workspace/WeldSmart_Pro/scripts/issue-tracker/issue_manager.py'
alias issq='iss query'
alias issl='iss list'
alias issp='iss query --status pending'
alias issf='iss query --status fixed'
alias iss-stats='iss stats'
```

使用：
```bash
issq --priority P2 --status pending
iss-stats
```

---

## 5. 故障排查

### 5.1 常见错误

#### 错误: `配置加载失败: FileNotFoundError`

**原因**: `config.yaml` 文件不存在

**解决**:
```bash
# 检查文件是否存在
ls scripts/issue-tracker/config.yaml

# 使用 -c 指定正确路径
python3 issue_manager.py -c /path/to/config.yaml stats
```

#### 错误: `编号 'X-001' 格式无效`

**原因**: 编号前缀不在 `config.yaml` 中定义

**解决**: 编辑 `config.yaml`，添加前缀定义：
```yaml
id_rules:
  prefixes:
    X: { priority: P2, label: "Extra" }
```

#### 错误: `编号 'M-037' 已存在`

**原因**: 尝试添加重复编号

**解决**:
- 使用 `update` 更新现有条目
- 或使用 `--force` 强制覆盖（migrate 时）

#### 错误: `gh 命令失败`

**原因**: `gh` CLI 未安装或未登录

**解决**:
```bash
# 安装 gh
# https://cli.github.com/

# 登录
gh auth login
```

### 5.2 数据库损坏

如果数据库损坏，重新迁移：

```bash
# 删除旧数据库
rm docs/project/issues.db

# 重新迁移
python3 scripts/issue-tracker/issue_manager.py migrate \
    --source docs/project/all-issues.md --migrator weldsmart
```

### 5.3 迁移结果不一致

如果迁移条目数与预期不符：

```bash
# 1. 检查警告
python3 scripts/issue-tracker/issue_manager.py migrate \
    --source docs/project/all-issues.md --migrator weldsmart --dry-run

# 2. 检查数据库
sqlite3 docs/project/issues.db "SELECT COUNT(*) FROM issues;"

# 3. 对比概要表
grep -c "^### [A-Z]-" docs/project/all-issues.md
```

---

## 6. 新项目接入指南

### 6.1 复制工具目录

```bash
cp -r scripts/issue-tracker /path/to/new-project/scripts/
```

### 6.2 修改 config.yaml

```yaml
project:
  name: "Your Project Name"
  db_path: "docs/project/issues.db"

id_rules:
  format: "{prefix}-{num:03d}"
  prefixes:
    # 根据项目定义前缀与优先级映射
    BUG: { priority: P0, label: "Bug" }
    FEAT: { priority: P1, label: "Feature" }
    REFAC: { priority: P2, label: "Refactor" }

priorities: [P0, P1, P2, P3]
statuses: [pending, in_progress, fixed, n_a]

github:
  enabled: true
  close_on_fix: true
  comment_template: "Auto-sync: {issue_id} fixed"

export:
  output: "docs/project/issues.md"
```

### 6.3 实现 Migrator 插件

创建 `migrators/yourproject_migrator.py`:

```python
from . import BaseMigrator
import re

class YourProjectMigrator(BaseMigrator):
    def parse(self, source_path: str) -> list[dict]:
        with open(source_path, 'r', encoding='utf-8') as f:
            content = f.read()

        issues = []
        # 实现项目特定的解析逻辑
        # ...

        return issues
```

### 6.4 测试

```bash
# Dry-run 预览
python3 issue_manager.py migrate \
    --source your-issues.md \
    --migrator yourproject \
    --dry-run

# 实际导入
python3 issue_manager.py migrate \
    --source your-issues.md \
    --migrator yourproject
```

---

## 7. 附录

### 7.1 状态符号对照表

| 符号 | 状态值 | 含义 |
|------|--------|------|
| ✅ | `fixed` | 已修复/已完成 |
| ❌ | `pending` | 待修复 |
| 🟢 | `in_progress` | 进行中 |
| 📋 | `planned` | 待规划 |
| ⚠️ | `n_a` | 不适用 |

### 7.2 编号前缀对照表 (WeldSmart Pro)

| 前缀 | 优先级 | 说明 |
|------|--------|------|
| C-xxx | P0 | Critical - 紧急 |
| H-xxx | P1 | High - 高优先级 |
| M-xxx | P2 | Medium - 中等 |
| L-xxx | P3 | Low - 低优先级 |
| A-xxx | P2 | Architecture - 架构问题 |
| T-xxx | P3 | Test - 测试问题 |

### 7.3 文件路径

| 文件 | 路径 | 说明 |
|------|------|------|
| CLI 入口 | `scripts/issue-tracker/issue_manager.py` | 主程序 |
| 配置文件 | `scripts/issue-tracker/config.yaml` | 项目配置 |
| 数据库 | `docs/project/issues.db` | 本地数据库 (gitignore) |
| 导出文件 | `docs/project/all-issues.md` | Markdown 导出 (git) |

### 7.4 命令速查表

| 命令 | 功能 | 常用参数 |
|------|------|---------|
| `add` | 新增 | `--id`, `--title`, `--priority` |
| `update` | 更新 | `--status`, `--fix-date` |
| `query` | 查询 | `--status`, `--priority`, `--detail` |
| `list` | 列表 | `--status` |
| `stats` | 统计 | 无 |
| `export` | 导出 | `--output` |
| `sync` | 同步 | `--dry-run` |
| `migrate` | 导入 | `--source`, `--migrator`, `--force` |

---

**文档结束**

如有问题，请参考 `技术设计方案.md` 或提交 Issue。

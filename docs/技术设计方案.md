# Issue Tracker CLI 工具 - 技术设计方案

> **版本**: 1.0
> **日期**: 2026-02-03
> **作者**: Claude Code
> **定位**: 通用开发工具，换项目只需替换 `config.yaml` 和对应的 migrator 即可复用

---

## 1. 概述

### 1.1 背景

将 `all-issues.md`（WeldSmart Pro 项目 132 个问题条目）从纯 Markdown 文档迁移到 SQLite 数据库，提供高效的查询、编辑、导出功能，同时支持与 GitHub Issue 单向同步。

### 1.2 核心设计目标

| 目标 | 说明 |
|------|------|
| **通用性** | 换项目只需替换 `config.yaml` 和 migrator 插件 |
| **零依赖** | 仅依赖 Python 标准库 + PyYAML |
| **双向同步** | MD → DB (migrate) + DB → MD (export) |
| **CLI 友好** | 支持脚本调用 + 直接 `sqlite3` 命令行访问 |
| **GitHub 集成** | 自动关闭已修复问题的 GitHub Issue |

### 1.3 技术栈

- **语言**: Python 3.10+
- **数据库**: SQLite3 (内置)
- **配置**: YAML (PyYAML)
- **测试**: unittest (内置)
- **外部依赖**: `gh` CLI (GitHub 同步功能)

---

## 2. 系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              Issue Tracker CLI                          │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │                        CLI 入口层                                 │  │
│  │  issue_manager.py (argparse + 命令分发)                          │  │
│  └───────────────────────────────────────────────────────────────────┘  │
│                                  │                                       │
│  ┌───────────────────────────────┼───────────────────────────────────┐  │
│  │                               │                                   │  │
│  ▼                               ▼                                   │  │
│  ┌───────────────────┐   ┌─────────────────────────────────────────┐  │
│  │   Config Loader   │   │           Core Modules                  │  │
│  │   config.yaml     │   │  ┌──────────┐  ┌─────────────────────┐  │  │
│  │                   │   │  │ Database │  │     Exporter        │  │  │
│  │  - 编号规则       │   │  │   (CRUD) │  │  (MD 生成)          │  │  │
│  │  - 优先级映射     │   │  └──────────┘  └─────────────────────┘  │  │
│  │  - 状态枚举       │   │  ┌──────────┐  ┌─────────────────────┐  │  │
│  │  - GitHub 配置    │   │  │  Github  │  │      Model          │  │  │
│  └───────────────────┘   │  │   Sync   │  │   (Issue dataclass) │  │  │
│                          │  └──────────┘  └─────────────────────┘  │  │
│  ┌───────────────────┐   └─────────────────────────────────────────┘  │
│  │  Migrator 插件    │                                                   │
│  │                   │                                                   │
│  │  BaseMigrator     │◄────── 项目特定实现                              │
│  │    (抽象基类)     │                                                   │
│  │                   │                                                   │
│  │  WeldSmart        │                                                   │
│  │  Migrator         │                                                   │
│  └───────────────────┘                                                   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                          数据持久化层                                    │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────────┐  │
│  │ issues.db        │  │ all-issues.md    │  │ GitHub Issues        │  │
│  │ (SQLite 本地DB)  │  │ (Git 持久化)     │  │ (gh CLI 同步)        │  │
│  └──────────────────┘  └──────────────────┘  └──────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 模块职责

| 模块 | 文件 | 职责 |
|------|------|------|
| **CLI 入口** | `issue_manager.py` | 参数解析、命令分发、错误处理 |
| **配置管理** | `core/config.py` | 加载校验、优先级推断、格式验证 |
| **数据访问** | `core/database.py` | SQLite CRUD、查询过滤、统计聚合 |
| **数据模型** | `core/model.py` | Issue 数据类定义 |
| **导出器** | `core/exporter.py` | DB → Markdown 转换 |
| **GitHub 同步** | `core/github_sync.py` | 调用 gh CLI、记录同步日志 |
| **迁移插件** | `migrators/*.py` | 源文件解析（项目特定） |

---

## 3. 数据模型设计

### 3.1 数据库 Schema

```sql
-- 核心问题表
CREATE TABLE IF NOT EXISTS issues (
    id              TEXT    PRIMARY KEY,       -- 问题编号 (如 C-001)
    title           TEXT    NOT NULL,          -- 标题
    priority        TEXT    NOT NULL,          -- 优先级 (P0/P1/P2/P3)
    status          TEXT    NOT NULL DEFAULT 'pending',  -- 状态
    discovery_date  TEXT    NOT NULL,          -- 发现日期 YYYY-MM-DD
    fix_date        TEXT,                       -- 修复日期 YYYY-MM-DD
    file_path       TEXT,                       -- 文件路径 (逗号分隔)
    location        TEXT,                       -- 位置描述
    description     TEXT,                       -- 问题描述
    impact          TEXT,                       -- 影响
    fix_plan        TEXT,                       -- 修复方案
    estimated_hours REAL,                       -- 预计工时
    actual_hours    REAL,                       -- 实际工时
    phase           TEXT,                       -- 所属阶段
    github_issue_id INTEGER,                   -- GitHub Issue 编号
    created_at      TEXT    DEFAULT (datetime('now')),
    updated_at      TEXT    DEFAULT (datetime('now'))
);

-- GitHub 同步日志表
CREATE TABLE IF NOT EXISTS github_sync_log (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    issue_id        TEXT    NOT NULL,
    github_issue_id INTEGER NOT NULL,
    action          TEXT    NOT NULL,          -- close / comment
    status          TEXT    NOT NULL,          -- success / failed
    error_msg       TEXT,
    synced_at       TEXT    DEFAULT (datetime('now')),
    FOREIGN KEY (issue_id) REFERENCES issues(id)
);

-- 索引
CREATE INDEX idx_priority        ON issues(priority);
CREATE INDEX idx_status          ON issues(status);
CREATE INDEX idx_discovery_date  ON issues(discovery_date);
CREATE INDEX idx_github          ON issues(github_issue_id);

-- 视图：待处理问题
CREATE VIEW v_pending AS
    SELECT id, title, priority, status, discovery_date, file_path
    FROM issues
    WHERE status IN ('pending', 'in_progress', 'planned')
    ORDER BY priority, discovery_date;

-- 视图：概要汇总
CREATE VIEW v_summary AS
    SELECT id, title, priority, status, discovery_date, fix_date, github_issue_id
    FROM issues
    ORDER BY id;
```

### 3.2 Issue 数据类

```python
@dataclass
class Issue:
    """问题条目数据类."""
    id: str                      # 问题编号
    title: str                   # 标题
    priority: str                # 优先级
    status: str                  # 状态
    discovery_date: str          # 发现日期
    fix_date: str | None = None  # 修复日期
    file_path: str | None = None # 文件路径
    location: str | None = None  # 位置
    description: str | None = None   # 描述
    impact: str | None = None        # 影响
    fix_plan: str | None = None      # 修复方案
    estimated_hours: float | None = None  # 预计工时
    actual_hours: float | None = None     # 实际工时
    phase: str | None = None              # 阶段
    github_issue_id: int | None = None    # GitHub Issue 编号
```

### 3.3 状态枚举

| 状态值 | 符号 | 含义 |
|--------|------|------|
| `fixed` | ✅ | 已修复/已完成 |
| `pending` | ❌ | 待修复 |
| `in_progress` | 🟢 | 进行中 |
| `planned` | 📋 | 待规划 |
| `n_a` | ⚠️ | 不适用 |

---

## 4. 核心模块设计

### 4.1 配置管理 (Config)

**职责**:
- 加载并校验 `config.yaml`
- 编号格式验证
- 优先级/状态枚举校验
- 前缀 → 优先级映射

**关键方法**:
```python
class Config:
    def __init__(self, config_path: str)
    def is_valid_id(self, issue_id: str) -> bool
    def is_valid_priority(self, priority: str) -> bool
    def is_valid_status(self, status: str) -> bool
    def priority_for_prefix(self, prefix: str) -> str
    def label_for_prefix(self, prefix: str) -> str
```

**配置结构**:
```yaml
project:
  name: "Project Name"
  db_path: "path/to/issues.db"

id_rules:
  format: "{prefix}-{num:03d}"
  prefixes:
    C: { priority: P0, label: "Critical" }
    H: { priority: P1, label: "High" }
    M: { priority: P2, label: "Medium" }
    L: { priority: P3, label: "Low" }

priorities: [P0, P1, P2, P3]
statuses: [pending, in_progress, planned, fixed, n_a]

github:
  enabled: true
  close_on_fix: true
  comment_template: "自动同步: {issue_id} 已修复"

export:
  output: "docs/project/all-issues.md"
```

### 4.2 数据访问层 (Database)

**职责**:
- SQLite 连接管理
- CRUD 操作封装
- 多条件查询构建
- 统计聚合

**关键方法**:
```python
class Database:
    def add_issue(self, issue: Issue) -> None
    def get_issue(self, issue_id: str) -> Issue | None
    def update_issue(self, issue_id: str, **fields) -> bool
    def upsert_issue(self, issue: Issue) -> None
    def delete_issue(self, issue_id: str) -> bool
    def issue_exists(self, issue_id: str) -> bool

    def query_issues(
        self, issue_id: str | None = None,
        priority: str | None = None,
        status: str | None = None,
        phase: str | None = None,
        file_glob: str | None = None,
        github_issue_id: int | None = None,
    ) -> list[Issue]

    def get_stats(self) -> dict
    def get_pending_github_sync(self) -> list[Issue]
    def log_github_sync(self, issue_id: str, gh_id: int,
                       action: str, status: str, error: str | None = None) -> None
```

### 4.3 导出器 (Exporter)

**职责**:
- 从数据库生成 Markdown 文档
- 保持与原 `all-issues.md` 格式兼容
- 统计汇总表生成

**输出结构**:
```markdown
# Project 问题清单汇总

> 生成日期: YYYY-MM-DD
> 总条目数: N | 已修复: X | 待处理: Y

## 目录
...

## 文档格式规范
...

## 总体统计
### 按优先级统计
### 问题概要汇总

## Critical Priority (P0)
### C-001: 标题 - ✅ 已修复
...

## High Priority (P1)
...

## 待修复问题优先级排序
...

## 附录：问题分类统计
...
```

### 4.4 GitHub 同步 (GithubSync)

**职责**:
- 单向同步: `status=fixed` → 关闭 GitHub Issue
- 调用 `gh issue close` CLI 命令
- 记录同步日志（避免重复同步）

**同步条件**:
```sql
SELECT * FROM issues
WHERE status = 'fixed'
  AND github_issue_id IS NOT NULL
  AND id NOT IN (
    SELECT issue_id FROM github_sync_log
    WHERE action = 'close' AND status = 'success'
  )
```

**命令格式**:
```bash
gh issue close <github_issue_id> --comment "自动同步: {issue_id} 已修复"
```

### 4.5 迁移插件 (Migrator)

**基类接口**:
```python
class BaseMigrator(ABC):
    @abstractmethod
    def parse(self, source_path: str) -> list[dict]:
        """解析源文件，返回 issue 字典列表."""
        raise NotImplementedError

    def validate(self, issues: list[dict]) -> list[str]:
        """数据完整性校验，返回警告信息列表."""
        ...
```

**WeldSmartMigrator 实现**:

解析 `all-issues.md` 的特定格式:
- 标题行: `### C-001: 标题 - ✅ 已修复/已完成`
- 单行字段: `**发现日期**: YYYY-MM-DD`
- 多行字段: `**问题描述**:` 后续行累积

**状态符号映射**:
```python
STATUS_SYMBOL_MAP = {
    "✅": "fixed",      # 已修复/已完成
    "❌": "pending",    # 待修复
    "⚠️": "n_a",        # 不适用
    "🟢": "in_progress", # 进行中
    "📋": "planned",    # 待规划
}
```

---

## 5. CLI 命令设计

### 5.1 命令列表

| 命令 | 功能 | 关键参数 |
|------|------|---------|
| `add` | 新增问题 | `--id`, `--title`, `--priority`, `--status` |
| `update` | 更新字段 | `--status`, `--fix-date`, `--actual-hours` |
| `query` | 多条件查询 | `--priority`, `--status`, `--file`, `--detail` |
| `list` | 简洁列表 | `--status`, `--priority` |
| `stats` | 统计概览 | 无 |
| `export` | 生成 MD | `--output` |
| `sync` | GitHub 同步 | `--dry-run` |
| `migrate` | 导入数据 | `--source`, `--migrator`, `--force` |

### 5.2 用法示例

```bash
# 新增问题
python3 issue_manager.py add \
    --id M-037 \
    --title "示例问题" \
    --priority P2 \
    --file "src/test.cpp"

# 更新状态
python3 issue_manager.py update M-037 --status fixed --fix-date 2026-02-03

# 查询
python3 issue_manager.py query --priority P2 --status pending --detail

# 统计
python3 issue_manager.py stats

# 导出
python3 issue_manager.py export

# GitHub 同步预览
python3 issue_manager.py sync --dry-run

# 迁移导入
python3 issue_manager.py migrate \
    --source all-issues.md \
    --migrator weldsmart
```

---

## 6. 通用化设计

### 6.1 通用 vs 项目特定

| 组成部分 | 是否通用 | 处理方式 |
|---------|---------|---------|
| 核心表结构 | ✅ 通用 | 直接用 |
| CLI 命令集 | ✅ 通用 | 直接用 |
| GitHub 同步逻辑 | ✅ 通用 | 直接用 |
| 编号体系 | ❌ 项目特定 | `config.yaml` |
| 优先级枚举 | ❌ 项目特定 | `config.yaml` |
| 状态枚举 | ❌ 项目特定 | `config.yaml` |
| 迁移脚本 | ❌ 项目特定 | migrator 插件 |
| Export 格式 | ❌ 项目特定 | 参数化 |

### 6.2 新项目接入步骤

1. **复制 `scripts/issue-tracker/` 目录**
2. **修改 `config.yaml`**:
   - `project.name`: 项目名称
   - `id_rules.prefixes`: 编号前缀与优先级映射
   - `priorities` / `statuses`: 枚举值
3. **实现 Migrator 插件**:
   - 继承 `BaseMigrator`
   - 实现 `parse()` 方法解析项目特定格式
4. **验证**: `python3 issue_manager.py migrate --dry-run`

---

## 7. 数据流与同步策略

### 7.1 MD ↔ DB 双向同步

```
┌─────────────────────────────────────────────────────────┐
│                    Git 仓库                              │
│  ┌─────────────────┐         ┌──────────────────────┐  │
│  │ all-issues.md   │         │ issues.db (gitignore)│  │
│  │ (持久化载体)    │         │ (本地工作环境)        │  │
│  └─────────────────┘         └──────────────────────┘  │
│         ▲                            ▲                 │
│         │                            │                 │
│         │ export                    │ migrate          │
│         │                            │                 │
│    ┌────┴────────────────────────────┴────┐            │
│    │         Issue Tracker CLI          │            │
│    └─────────────────────────────────────┘            │
└─────────────────────────────────────────────────────────┘
```

### 7.2 GitHub 单向同步

```
┌─────────────┐     status=fixed      ┌─────────────┐
│  本地 DB    │ ──────────────────►  │ GitHub Issue│
│  (issues.db)│   gh issue close     │    (远程)    │
└─────────────┘                      └─────────────┘
       │                                    │
       │ 记录同步日志                        │ 已关闭
       ▼                                    ▼
┌─────────────────┐              ┌─────────────┐
│ github_sync_log │              │   Closed    │
└─────────────────┘              └─────────────┘
```

---

## 8. 测试策略

### 8.1 单元测试覆盖

| 测试类 | 覆盖范围 | 用例数 |
|--------|---------|--------|
| `TestConfig` | 配置加载校验 | 5 |
| `TestDatabaseCRUD` | 数据库增删改查 | 10 |
| `TestDatabaseQuery` | 查询过滤 | 6 |
| `TestDatabaseStats` | 统计聚合 | 3 |
| `TestWeldSmartMigrator` | MD 解析 | 7 |
| `TestExporter` | Markdown 生成 | 4 |
| `TestGithubSync` | GitHub 同步 (mock) | 6 |
| `TestGithubSyncQuery` | 同步查询条件 | 4 |
| **总计** | | **46** |

### 8.2 运行测试

```bash
cd scripts/issue-tracker
python3 -m unittest discover -s tests -v
```

### 8.3 集成验证

```bash
# 1. 迁移导入
python3 issue_manager.py migrate --source all-issues.md --migrator weldsmart

# 2. 统计验证
python3 issue_manager.py stats

# 3. 查询验证
python3 issue_manager.py query --status pending

# 4. 导出验证
python3 issue_manager.py export --output /tmp/test-export.md

# 5. sqlite3 直接访问
sqlite3 issues.db "SELECT * FROM v_pending;"
```

---

## 9. 性能考虑

### 9.1 数据库规模

- 预期规模: < 10,000 条/项目
- 单条记录: ~500 bytes (含描述文本)
- 数据库大小: < 5 MB

### 9.2 查询优化

- 索引: `priority`, `status`, `discovery_date`, `github_issue_id`
- 视图: `v_pending` 常用查询预定义
- glob 匹配: 使用 SQL `GLOB` 而非 Python 后过滤

### 9.3 实时性要求

- 所有操作: < 100ms (千条规模)
- 导出操作: < 500ms
- 不需要连接池 (单线程 CLI)

---

## 10. 安全考虑

### 10.1 SQL 注入防护

- 使用参数化查询 (`?` 占位符)
- 不拼接 SQL 字符串

### 10.2 文件操作

- 数据库写入前校验路径
- 导出文件路径可配置

### 10.3 GitHub 同步

- 仅 `close` 操作，不创建/修改 Issue
- 失败记录日志，不重试避免误操作

---

## 11. 扩展点

### 11.1 新增 Migrator

```python
# migrators/custom_migrator.py
from . import BaseMigrator

class CustomMigrator(BaseMigrator):
    def parse(self, source_path: str) -> list[dict]:
        # 实现项目特定解析逻辑
        ...
```

### 11.2 自定义导出格式

继承 `Exporter` 并覆盖 `export()` 方法。

### 11.3 新增命令

在 `issue_manager.py` 中添加新的命令处理函数和 `argparse` 子解析器。

---

## 12. 依赖清单

### 12.1 Python 依赖

```
PyYAML>=6.0
```

### 12.2 外部工具依赖

```
gh (GitHub CLI) - 用于 GitHub 同步功能
sqlite3 (可选) - 用于直接命令行访问数据库
```

### 12.3 Python 版本

- **最低**: Python 3.10
- **推荐**: Python 3.11+

---

## 13. 附录

### 13.1 文件清单

```
scripts/issue-tracker/
├── config.yaml                  # 项目配置
├── issue_manager.py             # CLI 入口
├── core/
│   ├── __init__.py
│   ├── config.py                # 配置管理
│   ├── database.py              # 数据访问
│   ├── model.py                 # 数据模型
│   ├── exporter.py              # 导出器
│   └── github_sync.py           # GitHub 同步
├── migrators/
│   ├── __init__.py              # 基类
│   └── weldsmart_migrator.py    # WeldSmart 解析器
├── tests/
│   └── test_issue_manager.py    # 单元测试 (46 tests)
└── docs/
    ├── 技术设计方案.md            # 本文档
    └── 使用指导.md                # 用户手册
```

### 13.2 数据库路径

- **配置路径**: `config.yaml` 中 `project.db_path`
- **相对基准**: Git 仓库根目录 (自动解析)
- **默认位置**: `docs/project/issues.db`
- **Git 状态**: `.gitignore` (不入库)

### 13.3 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| 1.0 | 2026-02-03 | 初始版本，支持 WeldSmart Pro |

---

**文档结束**

project:
  id: "001"
  name: "WeldSmart Pro"

# 编号规则
id_rules:
  format: "{num:03d}"  # 全局自动递增序号

priorities: [P0, P1, P2, P3]  # 有序，高→低

statuses: [pending, in_progress, planned, fixed, n_a]

github:
  enabled: true
  close_on_fix: true
  comment_template: "自动同步: {issue_id} 已修复"

export:
  output: "exports/weldsmart_issues.md"  # 相对于 ISSUE_TRACKER_HOME
